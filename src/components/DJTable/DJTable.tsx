export const DJTable = () => {
    // Render two youtube videos, one left, one right
    // Add a volume slider for each
    // Add a crossfade slider, which will crossfade audio between the two videos

    return (
        <div>
            <h1>DJTable</h1>
        </div>
    )
}